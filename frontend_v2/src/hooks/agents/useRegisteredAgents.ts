/**
 * DEPRECATED shim. See useRunAgent.ts — the original raw-fetch implementation
 * was unauthenticated and returned the un-unwrapped envelope.
 */
export { useRegisteredAgents } from './useAgents'
